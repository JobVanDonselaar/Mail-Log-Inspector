<Window x:Class="MailLogInspector.App.MailHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mail Log Inspector - Volledige historie"
        Width="1080"
        Height="620"
        MinWidth="820"
        MinHeight="460"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="#F3F5F8"
        FontFamily="Segoe UI"
        FontSize="13">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <TextBlock Text="Volledige historie uit het archief" FontSize="20" FontWeight="SemiBold" Foreground="#14233A" />
      <TextBlock Name="SubtitleTextBlock" Foreground="#51657F" Margin="0,4,0,0" TextWrapping="Wrap" />
    </StackPanel>

    <Border Grid.Row="1" Background="White" BorderBrush="#CBD7E4" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,12">
      <StackPanel>
        <TextBlock Name="StatusTextBlock" Foreground="#14233A" TextWrapping="Wrap" />
        <ProgressBar Name="SearchProgressBar" Height="6" Margin="0,8,0,0" Minimum="0" Maximum="1" Visibility="Collapsed" />
      </StackPanel>
    </Border>

    <Border Grid.Row="2" Background="White" BorderBrush="#CBD7E4" BorderThickness="1" CornerRadius="6" Padding="0">
      <DataGrid Name="HistoryGrid"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                IsReadOnly="True"
                HeadersVisibility="Column"
                GridLinesVisibility="Horizontal"
                HorizontalGridLinesBrush="#E4EAF1"
                BorderThickness="0"
                RowHeight="30"
                ColumnHeaderHeight="30"
                FontSize="12">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Moment" Width="128" Binding="{Binding MomentDisplay}" />
          <DataGridTextColumn Header="Status" Width="150" Binding="{Binding StatusDisplay}" />
          <DataGridTextColumn Header="Code" Width="62" Binding="{Binding ResponseCodeDisplay}" />
          <DataGridTextColumn Header="Betekenis" Width="230" Binding="{Binding ResponseCodeMeaning}" />
          <DataGridTextColumn Header="Pogingen" Width="76" Binding="{Binding TriesDisplay}" />
          <DataGridTextColumn Header="Antwoord ontvangende server" Width="*" Binding="{Binding ResponseMessage}" />
        </DataGrid.Columns>
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow">
            <Setter Property="ToolTip" Value="{Binding ToolTipText}" />
          </Style>
        </DataGrid.RowStyle>
      </DataGrid>
    </Border>

    <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
      <TextBlock Name="FooterTextBlock" DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="#51657F" TextWrapping="Wrap" MaxWidth="700" />
      <Button Name="CloseButton" DockPanel.Dock="Right" Content="Sluiten" MinWidth="96" MinHeight="32" Click="CloseButton_Click" />
      <Button Name="CopyButton" DockPanel.Dock="Right" Content="Kopieer als tekst" MinWidth="130" MinHeight="32" Margin="0,0,8,0" Click="CopyButton_Click" />
    </DockPanel>
  </Grid>
</Window>
